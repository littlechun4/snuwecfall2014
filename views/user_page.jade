extends layout

block script
    script(src='/javascripts/logout.js')

block content
    div#message_box_container(class="text-center")
        p#message_box
            h3 Welcome #{user_name} 
            h3 You have logged in #{login_count} times
            div
                button#btnLogout(type="button" class="btn btn-default") Logout
