extends layout

block script
    script(src='/javascripts/login.js')

block content
    Please Enter your credentials below
    form#login_form(name="login", method="post", action=".")
        div
            input#username(type="text", placeholder="username", name="username")
        div
            input#userpassword(type="password", placeholder="password", name="userpassword")
        div
            button#btnLogin(type="submit") Login
            //button#btnSignUp(type="submit") Add User
